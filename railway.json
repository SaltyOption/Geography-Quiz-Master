{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "DATABASE_URL=$DATABASE_PUBLIC_URL pnpm --filter @workspace/scripts run check-db-image-files && DATABASE_URL=$DATABASE_PUBLIC_URL PORT=26064 BASE_PATH=/ pnpm --filter @workspace/geo-quiz run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "preDeployCommand": "pnpm --filter @workspace/db run push",
    "startCommand": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
